// 1. number
const getal = 5;

// 2. string
const woord = "brandweer";

// 3. boolean
const status = true;

// 4. array
const lijst = ["Bart", "Cindy", 8, true, false, "Bert"];

// 5. object
const persoon = {
  firstName: "Bart",
  lastName: "Verbomen",
  age: 34,
};

// 6. function

function sum(a, b) {
  return a + b;
}
